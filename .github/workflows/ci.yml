name: CI / CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── 1. CI: test + build + lint ─────────────────────────────────────────────
  ci:
    name: Test, Build & Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build
        env:
          # Dummy values so Next.js build doesn't fail on missing env vars
          NEXT_PUBLIC_CONTRACT_ID: ${{ secrets.NEXT_PUBLIC_CONTRACT_ID || 'CDIMCIKFTDYRMZNKG7XWJFYKN65JY43JYEUT4DLN3RHNGNQXRG52CV5L' }}
          NEXT_PUBLIC_REWARD_TOKEN_ID: ${{ secrets.NEXT_PUBLIC_REWARD_TOKEN_ID || 'CADMBCY6I6EK27FNYJMLKGDA6VUTTZJIB44NEJBLVPEXU3BGRBLGD4GO' }}
          NEXT_PUBLIC_SOROBAN_RPC_URL: https://soroban-testnet.stellar.org
          NEXT_PUBLIC_AMM_CONTRACT_ID: ${{ secrets.NEXT_PUBLIC_AMM_CONTRACT_ID || 'CDXQV5KJC2LGTCW7LKLEQKSHLEE4ODUGSEBOBRB6YVDIY73YEMCLOLSN' }}
          NEXT_PUBLIC_LP_TOKEN_ID: ${{ secrets.NEXT_PUBLIC_LP_TOKEN_ID || 'CADGL72YGVMJ7CD3IU6UNTYOGAQEMJ4AOGK5Q7QKYCHZTGEGF6K5FJDZ' }}
          NEXT_PUBLIC_TOKEN_A_ID: ${{ secrets.NEXT_PUBLIC_TOKEN_A_ID || 'CBWYMSLBEJDFVH4QIYV7VX2W26JWVEPMC7FU4PZPS5H62SUJKJ7V4TV2' }}
          NEXT_PUBLIC_TOKEN_B_ID: ${{ secrets.NEXT_PUBLIC_TOKEN_B_ID || 'CCOTCYJNSVFPNLCH3CASXSDM7IGFG23HB4PDSNZNKUUCUBLVQY3V5XTR' }}

      - name: Lint
        run: npm run lint

  # ── 2. CD: deploy to Vercel (only on push to main, after CI passes) ─────────
  deploy:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    needs: ci                    # wait for CI to pass first
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Deploy to Vercel (Production)
        run: npx vercel deploy --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
